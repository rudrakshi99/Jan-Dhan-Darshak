import React from "react";
import { View, Text } from "react-native";
import VoiceToText from "../VoiceToText";

const Help = () => {
	return (
		<View>
			<VoiceToText />
		</View>
	);
};

export default Help;
